"""Package marker for tooling.

This repo primarily runs via `main.py` (FastAPI app). The presence of this package allows
Poetry/uv editable installs to succeed without restructuring the codebase.
"""
